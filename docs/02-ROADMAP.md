# JAVIS 개발 로드맵

## 전체 타임라인

```
Phase 0: 환경 구축 ─────────────────────────────── Week 1
Phase 1: MVP (최소 동작 버전) ──────────────────── Week 2-3
Phase 2: 첫 파인튜닝 & 나만의 모델 ─────────────── Week 4-5
Phase 3: 도구 시스템 ──────────────────────────── Week 6-8
Phase 4: 메모리 & RAG ─────────────────────────── Week 9-11
Phase 5: 지속적 학습 파이프라인 ────────────────── Week 12-14
Phase 6: 고급 기능 ────────────────────────────── Week 15+
```

---

## Phase 0: 환경 구축 (Week 1)

### 목표
개발 환경과 클라우드 인프라 준비

### 작업 목록

```
[0-1] 로컬 개발 환경 세팅
      ├── Python 3.11+ 설치
      ├── 가상환경 생성 (venv 또는 conda)
      ├── 기본 패키지 설치
      └── IDE 설정 (VSCode 권장)

[0-2] 프로젝트 구조 생성
      ├── 디렉토리 구조 생성
      ├── pyproject.toml 설정
      ├── .gitignore 설정
      └── Git 초기화

[0-3] 클라우드 계정 설정
      ├── RunPod 계정 생성
      ├── API 키 발급
      ├── 결제 수단 등록
      └── Hugging Face 계정 (모델 다운로드용)

[0-4] 기본 테스트
      ├── RunPod에서 Qwen2.5-7B 테스트 배포
      ├── API 호출 테스트
      └── 응답 확인
```

### 산출물
- 동작하는 개발 환경
- RunPod 연결 확인
- 프로젝트 기본 구조

---

## Phase 1: MVP - 최소 동작 버전 (Week 2-3)

### 목표
터미널에서 클라우드 모델과 대화 가능한 기본 시스템

### 작업 목록

```
[1-1] 모델 클라이언트 구현
      ├── RunPod API 클라이언트
      ├── 요청/응답 처리
      ├── 에러 핸들링
      └── 타임아웃 처리

[1-2] 기본 대화 엔진
      ├── 시스템 프롬프트 관리
      ├── 대화 히스토리 관리
      ├── 메시지 포맷팅
      └── 스트리밍 응답 처리

[1-3] CLI 인터페이스
      ├── 입력/출력 처리
      ├── 명령어 파싱 (/exit, /clear 등)
      ├── 대화 표시 포맷
      └── 기본 설정 관리

[1-4] 설정 시스템
      ├── config.yaml 구조
      ├── 환경변수 관리 (.env)
      ├── 프로필 지원
      └── 설정 검증
```

### 산출물
```bash
# 실행 가능한 CLI
python -m javis

# 대화 예시
You: 안녕하세요
JAVIS: 안녕하세요! 무엇을 도와드릴까요?

You: /exit
Goodbye!
```

---

## Phase 2: 첫 파인튜닝 & 나만의 모델 (Week 4-5)

### 목표
나만의 스타일로 응답하는 첫 번째 커스텀 모델 생성

### 작업 목록

```
[2-1] 학습 데이터 준비
      ├── 데이터 포맷 정의 (ChatML)
      ├── 초기 데이터 50-100개 작성
      │   ├── 일반 대화 (20개)
      │   ├── 코딩 관련 (20개)
      │   ├── 작업 요청 (20개)
      │   ├── 도구 사용 예시 (20개)
      │   └── 엣지 케이스 (20개)
      └── 데이터 검증 스크립트

[2-2] 파인튜닝 환경 구축
      ├── 학습 스크립트 작성
      ├── 설정 파일 (QLoRA config)
      ├── RunPod 학습 환경 설정
      └── 체크포인트 관리

[2-3] 첫 파인튜닝 실행
      ├── 데이터 업로드
      ├── 학습 실행 (1-2시간)
      ├── 로그 모니터링
      └── 어댑터 다운로드

[2-4] 커스텀 모델 배포
      ├── LoRA 어댑터 업로드
      ├── RunPod 엔드포인트 설정
      ├── 베이스 + LoRA 통합
      └── 배포 테스트

[2-5] 평가 및 비교
      ├── 베이스 모델 vs 파인튜닝 모델 비교
      ├── 응답 품질 평가
      ├── 개선점 파악
      └── 다음 버전 계획
```

### 산출물
- `javis-lora-v0.1` 어댑터
- 배포된 나만의 모델 엔드포인트
- 평가 리포트

---

## Phase 3: 도구 시스템 (Week 6-8)

### 목표
외부 도구를 호출하여 실제 작업을 수행하는 능력

### 작업 목록

```
[3-1] 도구 프레임워크 구현
      ├── 도구 베이스 클래스
      ├── 도구 등록 시스템
      ├── 도구 호출 파싱
      └── 결과 처리

[3-2] 기본 도구 구현
      ├── 파일 도구 (읽기/쓰기/검색)
      ├── 쉘 도구 (명령 실행)
      ├── 웹 도구 (검색/스크래핑)
      └── 시스템 도구 (시간, 날씨 등)

[3-3] 도구 사용 학습
      ├── 도구 호출 데이터 50개 작성
      ├── 파인튜닝 (v0.2)
      └── 도구 사용 정확도 평가

[3-4] 멀티스텝 실행
      ├── 여러 도구 연쇄 호출
      ├── 중간 결과 처리
      └── 오류 복구
```

### 산출물
```python
# 도구 사용 예시
You: 현재 디렉토리의 Python 파일 목록 보여줘

JAVIS: [도구 호출: list_files(pattern="*.py")]
현재 디렉토리에 다음 Python 파일이 있습니다:
1. main.py
2. config.py
3. utils.py
```

---

## Phase 4: 메모리 & RAG (Week 9-11)

### 목표
대화 기억 및 개인 문서 검색 능력

### 작업 목록

```
[4-1] 단기 메모리
      ├── 대화 컨텍스트 관리
      ├── 요약 기능
      └── 컨텍스트 윈도우 최적화

[4-2] 장기 메모리
      ├── 벡터 DB 설정 (Chroma)
      ├── 대화 임베딩 저장
      ├── 관련 기억 검색
      └── 중요도 기반 필터링

[4-3] RAG 시스템
      ├── 문서 로더 (PDF, MD, TXT 등)
      ├── 청크 분할 전략
      ├── 임베딩 모델 선택
      ├── 검색 파이프라인
      └── 프롬프트 주입

[4-4] RAG 활용 학습
      ├── RAG 활용 데이터 작성
      ├── 파인튜닝 (v0.3)
      └── 검색 품질 평가
```

### 산출물
```python
# RAG 사용 예시
You: 지난주에 작성한 API 설계 문서 내용 요약해줘

JAVIS: [검색: API 설계 문서]
지난주 작성하신 API 설계 문서 요약입니다:
- REST API 엔드포인트 15개 정의
- 인증 방식: JWT
- 주요 리소스: users, projects, tasks
...
```

---

## Phase 5: 지속적 학습 파이프라인 (Week 12-14)

### 목표
사용하면서 자동으로 데이터 수집 및 모델 개선

### 작업 목록

```
[5-1] 피드백 수집 시스템
      ├── 응답 평가 인터페이스 (👍/👎)
      ├── 수정 응답 저장
      ├── 피드백 데이터 포맷팅
      └── 품질 필터링

[5-2] 데이터 관리
      ├── 버전별 데이터 관리
      ├── 데이터 증강 스크립트
      ├── 중복 제거
      └── 품질 점수 시스템

[5-3] 자동 학습 파이프라인
      ├── 학습 트리거 (데이터 N개 축적 시)
      ├── 자동 파인튜닝 스크립트
      ├── 모델 평가 자동화
      └── 자동 배포 (승인 후)

[5-4] A/B 테스트
      ├── 이전 버전 vs 새 버전 비교
      ├── 성능 메트릭 수집
      └── 롤백 메커니즘
```

### 산출물
```
자동 학습 사이클:

[사용] → [피드백] → [100개 축적] → [자동 학습] → [평가] → [배포]
   ↑                                                      │
   └──────────────────────────────────────────────────────┘
```

---

## Phase 6: 고급 기능 (Week 15+)

### 목표
완전한 개인 AI 비서 시스템

### 작업 목록

```
[6-1] 음성 인터페이스
      ├── STT (Whisper 로컬 또는 API)
      ├── TTS (Piper/Edge TTS)
      ├── 음성 대화 모드
      └── 웨이크 워드 (선택)

[6-2] 자동화 워크플로우
      ├── 워크플로우 정의 DSL
      ├── 스케줄러
      ├── 트리거 시스템
      └── 알림

[6-3] 외부 서비스 연동
      ├── 캘린더 (Google/Outlook)
      ├── 이메일
      ├── 메신저 (Slack/Discord)
      └── 커스텀 API

[6-4] 웹 인터페이스 (선택)
      ├── React/Next.js 프론트엔드
      ├── REST API
      └── 실시간 통신 (WebSocket)

[6-5] 모델 고도화
      ├── DPO 적용
      ├── 베이스 모델 업그레이드 (14B, 32B)
      ├── 특화 모델 (코딩, 분석 등)
      └── 멀티모달 (이미지 이해)
```

---

## 마일스톤 요약

| 마일스톤 | 기간 | 핵심 결과물 |
|----------|------|------------|
| M0 | Week 1 | 환경 구축 완료 |
| M1 | Week 3 | CLI 대화 가능 |
| M2 | Week 5 | **나만의 첫 모델 (v0.1)** |
| M3 | Week 8 | 도구 사용 가능 (v0.2) |
| M4 | Week 11 | RAG 작동 (v0.3) |
| M5 | Week 14 | 자동 학습 파이프라인 |
| M6 | Week 15+ | 완전한 자비스 |

---

## 다음 단계

Phase 0부터 시작:
1. 개발 환경 세팅
2. 프로젝트 구조 생성
3. RunPod 계정 설정
