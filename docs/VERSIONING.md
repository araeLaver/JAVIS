# JAVIS 모델 버전업 전략

## 버전업 사이클

```
┌─────────────────────────────────────────────────────────────────┐
│                      버전업 사이클                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐ │
│   │  v0.0    │───►│  v1.0    │───►│  v1.1    │───►│  v2.0    │ │
│   │ (Groq)   │    │(1차 튜닝)│    │(개선)    │    │(2차 튜닝)│ │
│   └──────────┘    └──────────┘    └──────────┘    └──────────┘ │
│        │               │               │               │       │
│        ▼               ▼               ▼               ▼       │
│   [데이터 수집]   [데이터 수집]   [데이터 수집]   [데이터 수집]  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 단계별 설명

### Phase 0: 데이터 수집 (현재)
- **모델**: Groq API (llama-3.1-8b-instant) - 무료
- **목표**: 학습 데이터 축적
- **할 일**:
  1. JAVIS와 평소처럼 대화
  2. 좋은 응답에 👍 피드백
  3. 나쁜 응답에 👎 피드백 (이건 학습에서 제외하거나 수정)
- **목표 데이터량**: 최소 500~1000개 대화 턴

### Phase 1: 첫 파인튜닝 (v1.0)
- **베이스 모델**: Qwen2.5-7B-Instruct
- **방법**: QLoRA (4bit 양자화 + LoRA)
- **학습 데이터**: feedback="good"인 대화만 선별
- **결과물**: LoRA adapter 파일 (~100MB)

### Phase 2: 배포 및 운영 (v1.0 런칭)
- **플랫폼**: RunPod Serverless
- **구성**: vLLM + Qwen2.5-7B + LoRA adapter
- **교체**: Groq → 자체 모델로 client.py 수정

### Phase 3: 지속적 개선 (v1.1, v1.2, ...)
- 계속 대화하며 새 데이터 수집
- 주기적으로 재학습 (새 데이터 포함)
- adapter만 교체하면 됨 (베이스 모델 동일)

---

## 모델 버전 관리

### 폴더 구조
```
models/
├── v1.0/
│   ├── adapter/
│   │   ├── adapter_config.json
│   │   └── adapter_model.safetensors
│   └── metadata.json
├── v1.1/
│   ├── adapter/
│   └── metadata.json
└── v2.0/
    ├── adapter/
    └── metadata.json
```

### metadata.json 형식
```json
{
  "version": "1.0",
  "base_model": "Qwen/Qwen2.5-7B-Instruct",
  "created_at": "2025-01-15",
  "training_data": {
    "total_conversations": 523,
    "total_turns": 2341,
    "date_range": "2025-12-18 ~ 2025-01-14"
  },
  "training_config": {
    "method": "qlora",
    "epochs": 3,
    "learning_rate": 2e-4,
    "lora_r": 64,
    "lora_alpha": 16
  },
  "notes": "첫 번째 파인튜닝 버전"
}
```

---

## 버전 교체 방법

### 1. 로컬 테스트
```python
# client.py에서 adapter 경로 변경
adapter_path = "models/v1.1/adapter"
```

### 2. RunPod 배포
```bash
# 새 adapter를 HuggingFace에 업로드
huggingface-cli upload your-username/javis-adapter-v1.1 models/v1.1/adapter

# RunPod 환경변수 수정
ADAPTER_PATH=your-username/javis-adapter-v1.1
```

### 3. 롤백
문제 발생 시 이전 버전으로 즉시 롤백 가능
```
ADAPTER_PATH=your-username/javis-adapter-v1.0
```

---

## 학습 데이터 품질 관리

### 좋은 데이터 (학습에 포함)
- feedback = "good"
- 자연스러운 대화
- 정확한 정보
- 원하는 스타일의 응답

### 나쁜 데이터 (제외 또는 수정)
- feedback = "bad"
- 할루시네이션 (잘못된 정보)
- 부적절한 응답
- 의도와 다른 답변

### 데이터 정제 워크플로우
```
1. /api/export-training?feedback_filter=good
   → 좋은 응답만 내보내기

2. 수동 검토 (선택사항)
   → JSONL 파일 열어서 이상한 거 삭제

3. 파인튜닝 실행
```

---

## 예상 일정

| 단계 | 기간 | 목표 |
|------|------|------|
| 데이터 수집 | 2~4주 | 500+ 대화 턴 |
| 첫 파인튜닝 | 1일 | v1.0 모델 생성 |
| 테스트/배포 | 1~2일 | RunPod 배포 |
| v1.1 개선 | 2주 후 | 추가 데이터로 재학습 |

---

## 비용 예상

| 항목 | 비용 |
|------|------|
| 현재 (Groq) | 무료 |
| 파인튜닝 (Google Colab Pro) | ~$10/월 |
| 파인튜닝 (RunPod GPU) | ~$0.5~1/시간 |
| 운영 (RunPod Serverless) | 사용한 만큼 (~$0.00025/초) |

실제 사용량이 많지 않으면 월 $5~10 수준으로 운영 가능.
